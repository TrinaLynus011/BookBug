apiVersion: v1
kind: Service
metadata:
  name: bookbee-frontend-service
  namespace: bookbee
spec:
  selector:
    app: bookbee-frontend
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
