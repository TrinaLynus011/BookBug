apiVersion: v1
kind: Service
metadata:
  name: bookbug-backend-service
  namespace: bookbug
spec:
  selector:
    app: bookbug-backend
  type: NodePort
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
