apiVersion: v1
kind: Service
metadata:
  name: bookbee-backend-service
  namespace: bookbee
spec:
  selector:
    app: bookbee-backend
  type: NodePort
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
