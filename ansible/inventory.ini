# BookBug Ansible Inventory
# Replace <HOST_IP> with your server's public IP (e.g. from Terraform output)
# Replace <SSH_KEY_PATH> with the path to your private key

[app_servers]
bookbug-host ansible_host=127.0.0.1 ansible_connection=local